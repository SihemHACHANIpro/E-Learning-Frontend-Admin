import { ComponentFixture, TestBed } from '@angular/core/testing';

import { UpdatechefdeprojrtComponent } from './updatechefdeprojrt.component';

describe('UpdatechefdeprojrtComponent', () => {
  let component: UpdatechefdeprojrtComponent;
  let fixture: ComponentFixture<UpdatechefdeprojrtComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [UpdatechefdeprojrtComponent]
    });
    fixture = TestBed.createComponent(UpdatechefdeprojrtComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
